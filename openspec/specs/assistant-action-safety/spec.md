## Purpose

Define safety constraints for assistant-triggered mutations.

> **Deprecated**: Assistant mutation execution is being removed entirely. The allowlist contract is no longer needed. Remove assistant action parsing/execution entry points and keep validation requirements on standard non-assistant APIs.
