## Purpose

Specify MCP surface completeness and semantics parity for all TUI-visible operations.

> **Deprecated**: MCP is being retired as an interop surface in favor of the regular CLI command surface (`cli-command-surface`). Use CLI command groups for task/project/goal/reminder/assistant operations, with `--json` for machine-readable output.
