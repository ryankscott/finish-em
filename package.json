{
  "name": "finish-em-mono",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:web": "cd packages/finish-em-web && npm run build",
    "install:web": "cd packages/finish-em-web && npm run install"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.0.4",
    "typescript-node": "^0.1.3"
  }
}
